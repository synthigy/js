# Everything here is a build artifact — regenerated by `npm run codegen` from the
# committed contract snapshots (synthigy/schema.json + synthigy/ops.ir.json).
# Commit the inputs, not the output. `prepare` regenerates on install.
*
!.gitignore
